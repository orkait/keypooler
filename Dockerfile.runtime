FROM alpine:3.19

RUN apk add --no-cache python3 go bash curl

# Install bun (handles both JS and TS)
RUN curl -fsSL https://bun.sh/install | bash
ENV PATH="/root/.bun/bin:$PATH"

# Verify runtimes
RUN python3 --version && bun --version && go version

WORKDIR /workspace
