services:
  keypooler:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${SERVER_PORT:-8080}:8080"
    env_file:
      - .env
    volumes:
      - pool-data:/app/data
    restart: unless-stopped

volumes:
  pool-data:
